import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  tabButtonStyle: {alignItems: 'center', justifyContent: 'center'},
  dotStyle: {position: 'absolute', right: 12},
});

export default styles;
